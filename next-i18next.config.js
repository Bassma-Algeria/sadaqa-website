module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'fr', 'ar'],
  },

  // remove it in production (this make the page reload every time we change the json file)
  // reloadOnPrerender: true,
};
