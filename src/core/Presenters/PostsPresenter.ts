import { Post } from '../CoreGateways/PostsGateway/PostsGateway.types';

class PostsPresenter {
  // responseModelsToViewModels(posts: PostResponseModel[]): Post[] {
  //   return posts.map(post => this.responseModelToViewModel(post));
  // }
  // responseModelToViewModel(post: PostResponseModel): Post {}
}

export { PostsPresenter };
